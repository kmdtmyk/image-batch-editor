{
  "name": "image-divider",
  "version": "0.0.0",
  "description": "image-divider",
  "main": "index.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . ImageDivider --platform=win32 --arch=x64 --overwrite --electron-version=1.6.2 --asar=true",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "babel": "^6.23.0",
    "babel-preset-es2015": "^6.24.1",
    "babel-register": "^6.24.1",
    "electron": "^1.6.2"
  },
  "devDependencies": {
    "electron-packager": "^8.6.0"
  }
}
